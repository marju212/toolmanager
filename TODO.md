1. Use this as default, no need to set them in config if it not differ from this.
DEFAULT_BRANCH=main      # branch you release from
TAG_PREFIX=v             # tags will look like v1.2.3
REMOTE=origin            # git remote name


2. Remove this
Keep config file at 600 permissions, if we use config file it should be in the repo





